# AmigaGuide Editor

The editor is a separate Qt application in the AmigaGuide on Linux repository. It shares the reusable `amigaguide` core with the reader.

The first implementation milestone is tracked by GitHub issue #18.

This directory is intentionally small at the start; editor GUI implementation will be added incrementally rather than forking reader logic.