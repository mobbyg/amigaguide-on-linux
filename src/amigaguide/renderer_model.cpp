#include "amigaguide/renderer_model.h"

namespace amigaguide {
// Rendering is deliberately kept separate from the parser. Qt formatting will
// be added in the viewer layer once parser compatibility is established.
}
